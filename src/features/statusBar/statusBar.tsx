import React from 'react';
import { Col } from 'react-bootstrap';  // Importing Bootstrap components
import './statusBar.scss';

// Custom vertical status bar component
const StatusBar: React.FC = () => (
  <div className="vertical-status-bar">
    {/* This is the status bar itself */}
  </div>
);

export default StatusBar;